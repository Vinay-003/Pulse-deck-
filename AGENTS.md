Before starting any task, read `.graphify/graph.json` and `.graphify/GRAPH_REPORT.md` to understand the codebase structure, communities, god nodes, and relationships.

After completing any task that modifies code, run `graphify update .` to rebuild the knowledge graph.

Use `/graphify query "<question>"` to explore the graph when you need to understand relationships between components.
